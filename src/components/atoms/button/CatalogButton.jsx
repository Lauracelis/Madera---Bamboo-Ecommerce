import React, { Component } from "react";
import { Button } from "@material-ui/core";
import { Link, Router } from 'react-router-dom'

export default function CatalogButton(){
    return(
        
        <Button  variant="contained" > VER CATÁLOGO</Button>
        
       
    )
}