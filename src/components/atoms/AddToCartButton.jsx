import React from "react";
import { Button } from "@mui/material";

export default function AddButton(){
    return(
        <Button color="primary" variant="contained" > AÑADIR AL CARRITO</Button>
    )
}